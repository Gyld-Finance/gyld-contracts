# Retrospective index

- GLD-264: Implement IERC-1643 (document management) on GyldBondToken — 2026-08-17 — 0 rework cycles (0 build failures, review PASS first pass, ~25m) — key learnings: rollout-stage artifacts (upgrade script, DEPLOYMENTS rollout entries, invariant handler) scoped in the plan were not delivered by the implementer and had to be re-enumerated by the reviewer for the rollout ticket — hand deploy/rollout artifacts to the rollout ticket explicitly; an immutable-logic factory (TokenFactory) means a UUPS implementation upgrade only lifts existing proxies, so new series need a new factory at the new logic (verified at research, must land in rollout); upgradeable-storage layout safety is proved by append-only struct + extended slot-pin raw-storage offsets + an upgrade-preservation test, never a comment. Extracted skill `upgradeable-storage-layout-safety` (bound to Implementer + Reviewer).